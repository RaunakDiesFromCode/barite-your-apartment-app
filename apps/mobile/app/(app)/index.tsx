import { View, Text } from 'react-native';

export default function AppHome() {
    return (
        <View className="flex-1 items-center justify-center">
            <Text className="text-xl font-bold">Logged in 🎉</Text>
        </View>
    );
}
